import {StyleSheet} from 'react-native';

const commomStyle = StyleSheet.create({
  loaderStyle: {
    flex: 1,
    alignItems: 'center',
    alignContent: 'center',
    alignSelf: 'center',
    height: 200,
  },
  loadingInfo: {
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default commomStyle;
